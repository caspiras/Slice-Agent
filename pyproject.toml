[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "slice"
version = "1.6.1"
description = "A local-first IDE wrapper for Ollama models. Chat naturally. Edit code with diffs. Read/write ALL documents (PDF, Word, Excel, etc.). All permission-gated."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "rich>=13.0.0",
    "ollama>=0.1.0",
    "prompt-toolkit>=3.0.0",
    "pypdf>=4.0.0",
    "reportlab>=4.0.0",
    "python-docx>=1.0.0",
    "openpyxl>=3.0.0",
    "python-pptx>=0.6.0",
    "pandas>=2.0.0",
    "tabulate>=0.9.0",
    "certifi>=2023.0.0",
]

[project.scripts]
slice = "slice.main:main"

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
]

[tool.black]
line-length = 100
target-version = ["py39"]

[tool.ruff]
line-length = 100
target-version = "py39"
